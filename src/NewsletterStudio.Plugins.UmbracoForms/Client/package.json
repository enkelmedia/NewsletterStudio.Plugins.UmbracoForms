{
  "name": "ns-plugin",
  "private": true,
  "version": "15.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build --emptyOutDir",
    "watch": "tsc && vite build --watch",
    "build:dev": "tsc && vite build --mode development --emptyOutDir",
    "generate:api": "openapi-ts"
  },
  "devDependencies": {
    "@hey-api/openapi-ts": "^0.48.3",
    "@newsletterstudio/umbraco": "^15.0.0",
    "@umbraco-cms/backoffice": "^15.0.0",
    "lit": "^3.2.0",
    "typescript": "^5.6.3",
    "vite": "^5.2.0"
  }
}
