{
  "name": "ns-plugin",
  "private": true,
  "version": "16.0.0-rc1",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build --emptyOutDir",
    "watch": "tsc && vite build --watch",
    "build:dev": "tsc && vite build --mode development --emptyOutDir",
    "generate:api": "openapi-ts"
  },
  "devDependencies": {
    "@hey-api/client-fetch": "^0.10.1",
    "@hey-api/openapi-ts": "^0.67.6",
    "@newsletterstudio/umbraco": "^16.0.0",
    "@umbraco-cms/backoffice": "^16.0.0",
    "lit": "^3.3.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
